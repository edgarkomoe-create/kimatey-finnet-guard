pandas>=2.0
numpy>=1.26
scikit-learn==1.8.0
matplotlib>=3.7
plotly>=5.18
openpyxl>=3.1
joblib>=1.3
streamlit>=1.36
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
google-genai>=1.0
# Pilote PostgreSQL - utilise uniquement si la variable d'environnement DATABASE_URL est
# configuree (ex. Neon, Supabase, Render Postgres). Sans elle, repli automatique sur le
# stockage fichier JSON existant (voir core/db.py) - aucune rupture en local sans base.
psycopg[binary]>=3.1

# Optionnel : necessaire uniquement si AUTH_MODE=firebase avec un vrai projet Firebase
# (voir README, section Authentification). Non installe par defaut.
# firebase-admin>=6.0
